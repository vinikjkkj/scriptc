'use strict'
// Identical, plus an unused node:http edge — the ONE the install predicate
// actually tests. With this line the same net shim registers and isIP works.
const net = require('net')
const http = require('http')
void http
module.exports = {
  isip() { return String(net.isIP('1.2.3.4')) },
}
