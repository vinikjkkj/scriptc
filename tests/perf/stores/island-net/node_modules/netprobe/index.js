'use strict'
// An embedded npm package whose ONLY builtin edge is node:net. cc.ts links
// scr_net_island.c on this edge; the emitted main does not install it.
const net = require('net')
module.exports = {
  isip() { return String(net.isIP('1.2.3.4')) },
}
