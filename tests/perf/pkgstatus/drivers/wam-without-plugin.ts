// Driver B: the same entry MINUS the plugin factory. Everything else wam's
// index exports that has a value form.
import { WaWamCoordinator } from '../pkgs/wam/index'
console.log('coordinator ctor:', typeof WaWamCoordinator)
