// Control: a SUPPORTED method on the same ambient receiver. If the ambient read
// throws first, the method's support status is irrelevant -- nothing is reached.
declare const s: string
console.log('u=' + s.toUpperCase())
