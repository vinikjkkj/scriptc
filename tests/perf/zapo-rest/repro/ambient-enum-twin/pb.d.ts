export declare namespace pb {
    namespace Msg {
        enum Kind {
            UNKNOWN = 0,
            TEXT = 1,
            IMAGE = 2,
            AUDIO = 3,
        }
        enum Flavor {
            PLAIN = "plain",
            RICH = "rich",
        }
    }
}
