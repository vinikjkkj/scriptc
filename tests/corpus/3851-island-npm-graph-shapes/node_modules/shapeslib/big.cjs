// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
// repetitive banner line kept identical so raw DEFLATE beats the plain text
module.exports.padWidth = 7;
module.exports.padded = function padded(s) { return String(s).padEnd(module.exports.padWidth, "."); };
