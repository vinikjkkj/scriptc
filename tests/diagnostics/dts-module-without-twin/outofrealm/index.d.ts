// The generated-module shape: types here, values in index.js beside it.
// Its own package.json puts it OUTSIDE the entry's package realm, so the
// declaration-twin hiding (projectDtsRuntimeSibling) declines and
// resolution answers this file — the twin never joins the program.
export interface Row {
    readonly name: string
    readonly n: number
}
export declare const ROWS: readonly Row[]
