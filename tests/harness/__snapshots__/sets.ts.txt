sets.ts:12:7 - error SC2009: values of type 'Set<boolean>' cannot be compiled: the Set shape is supported, but elements are limited to numbers and strings — 'boolean' is outside that domain

  11 | // element type.
  12 | const byFlag = new Set<boolean>();
     |       ^~~~~~
  13 | 

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

sets.ts:12:16 - error SC1090: Set elements of type 'boolean' (Set elements must be string or number — Map's key kinds — or a server handle, which stores under reference identity) is not supported yet

  11 | // element type.
  12 | const byFlag = new Set<boolean>();
     |                ^~~~~~~~~~~~~~~~~~
  13 | 

sets.ts:19:17 - error SC2009: values of type 'Set<boolean>' cannot be compiled: the Set shape is supported, but elements are limited to numbers and strings — 'boolean' is outside that domain

  18 | // Set-typed slots elsewhere report the ordinary unsupported-type diagnostic.
  19 | function useBad(s: Set<boolean>): number {
     |                 ^
  20 |   return s.size;

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

sets.ts:33:10 - error SC2009: values of type 'Set<string> | Set<number>' cannot be compiled: the union shape is supported, but a union carrying two Set arms has no runtime test to tell 'Set<string>' from the other one ('instanceof Set' answers true for both, and 'typeof' answers "object" for every arm here)

  32 | // separates a `Set<string>` from a `Set<number>` at run time.
  33 | function twoSets(cond: boolean): Set<string> | Set<number> {
     |          ^~~~~~~
  34 |   return cond ? new Set<string>() : new Set<number>();

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

sets.ts:39:10 - error SC2009: values of type 'string | Map<string, number>' cannot be compiled: the union shape is supported, but 'Map<string, number>' arms have no home in a compiled union yet (no runtime narrowing test exists against sibling arms)

  38 | // `instanceof Map`, so the rule would be ahead of the test.
  39 | function mapArm(cond: boolean): string | Map<string, number> {
     |          ^~~~~~
  40 |   return cond ? "s" : new Map<string, number>();

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

sets.ts:49:28 - error SC1090: JSON.stringify of 'Set<string>' values (only number, string, boolean, records, arrays, unions of those, and 'unknown' stringify) are not supported yet

  48 | const s = new Set<string>();
  49 | console.log(JSON.stringify(s));
     |                            ^
  50 | 

sets.ts:52:15 - error SC1090: Set methods as values (call 'add' directly) are not supported yet

  51 | // Set methods have no bound-value form — call them directly.
  52 | const adder = s.add;
     |               ^~~~~
  53 | 

sets.ts:56:1 - error SC2004: uses of 'useBad' inherit the blocker on its declaration

  55 | // them relevant; these references are what makes them count.
  56 | useBad(new Set<boolean>());
     | ^~~~~~
  57 | maybeSet(true);

  hint: the declaration of 'useBad' did not compile — fix the diagnostic reported there and these sites clear with it

sets.ts:58:1 - error SC2004: uses of 'twoSets' inherit the blocker on its declaration

  57 | maybeSet(true);
  58 | twoSets(true);
     | ^~~~~~~
  59 | mapArm(true);

  hint: the declaration of 'twoSets' did not compile — fix the diagnostic reported there and these sites clear with it

sets.ts:59:1 - error SC2004: uses of 'mapArm' inherit the blocker on its declaration

  58 | twoSets(true);
  59 | mapArm(true);
     | ^~~~~~
  60 | 

  hint: the declaration of 'mapArm' did not compile — fix the diagnostic reported there and these sites clear with it