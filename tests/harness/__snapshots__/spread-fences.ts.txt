spread-fences.ts:16:17 - error SC1090: spread arguments into fixed parameter positions (a spread can only fill a rest parameter) are not supported yet

  15 | const pair: number[] = [1, 2];
  16 | console.log(two(...(pair as [number, number])));
     |                 ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  17 | 

spread-fences.ts:24:21 - error SC1090: conditional spreads beyond `...(c ? { field: v } : {})` (exactly one property against an empty arm — spell other shapes as optional fields) is not supported yet

  23 | const cond = true;
  24 | const s1: Shape = { ...(cond ? { x: 1, y: 2 } : { x: 3, y: 4 }) };
     |                     ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  25 | interface YOnly {

spread-fences.ts:42:32 - error SC1090: spread of 'x' over an earlier contributor whose evaluation is observable (the desugar keeps one entry per name — give 'x' one contributor, or move the spread first) is not supported yet

  41 | const part: OptXY = { x: 9 };
  42 | const s3: OptXY = { x: bump(), ...part };
     |                                ^~~~~~~
  43 | console.log(s3.x ?? -1, ticks);