node-fallback-fence.ts:19:1 - error SC2020: 'process.stdin.setEncoding' is part of the standard library types but has no scriptc lowering yet

  18 | process.off("exit", () => {});
  19 | process.stdin.setEncoding("utf8"); // stays fenced: chunks are bytes
     | ^~~~~~~~~~~~~~~~~~~~~~~~~
  20 | const buf = Buffer.from("x", "utf8").reverse(); // from and fill lower; reverse fences

  hint: isTTY, destroy(), on/once of the data/end/error events, and `for await (const chunk of process.stdin)` are the supported stdin surface (or read everything at once: readFileSync(0, "utf8") from node:fs)

node-fallback-fence.ts:20:13 - error SC2020: 'Buffer<ArrayBufferLike>.reverse' is part of the standard library types but has no scriptc lowering yet

  19 | process.stdin.setEncoding("utf8"); // stays fenced: chunks are bytes
  20 | const buf = Buffer.from("x", "utf8").reverse(); // from and fill lower; reverse fences
     |             ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  21 | const enc = new TextEncoder().encode("x");

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

node-fallback-fence.ts:23:16 - error SC2012: 'AbortSignal.timeout' runs in the embedded dynamic engine, which this build does not include

  22 | const dec = new TextDecoder().decode(enc);
  23 | const signal = AbortSignal.timeout(1000);
     |                ^~~~~~~~~~~~~~~~~~~~~~~~~
  24 | const res = fetch("https://example.com", { signal });

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

node-fallback-fence.ts:31:49 - error SC2020: 'Buffer.toString with this encoding' is part of the standard library types but has no scriptc lowering yet

  30 | const tmp = mkdtempSync("/tmp/scr-");
  31 | const raw = readFileSync("/etc/hosts").toString(tty ? "hex" : "latin1"); // the read and every literal encoding lower; a non-literal fences
     |                                                 ^~~~~~~~~~~~~~~~~~~~~~
  32 | 

  hint: a literal "utf8", "hex", "base64", "base64url", "latin1", "binary", "ascii", "utf16le", or "ucs2" spelling is the lowered encoding surface

node-fallback-fence.ts:34:28 - error SC2020: 'deflateSync of 'string' data' is part of the standard library types but has no scriptc lowering yet

  33 | import { deflateSync, gzipSync } from "node:zlib";
  34 | const packed = deflateSync("data"); // string data: the wrap-it-first hint
     |                            ^~~~~~
  35 | const zipped = gzipSync("data"); // beyond the lowered pair: fenced

  hint: zlib works on Buffers: deflateSync(Buffer.from(s, "utf8"))

node-fallback-fence.ts:35:25 - error SC2020: 'gzipSync of 'string' data' is part of the standard library types but has no scriptc lowering yet

  34 | const packed = deflateSync("data"); // string data: the wrap-it-first hint
  35 | const zipped = gzipSync("data"); // beyond the lowered pair: fenced
     |                         ^~~~~~
  36 | 

  hint: zlib works on Buffers: gzipSync(Buffer.from(s, "utf8"))