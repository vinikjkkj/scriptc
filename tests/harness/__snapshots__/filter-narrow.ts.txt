filter-narrow.ts:9:17 - error SC1090: '.filter' narrowing 'number | string | undefined' elements to the multi-arm 'number | string' (only a SINGLE arm re-tags — keep the receiver's element type instead — annotate the callback's return ': boolean' (the checker then skips the predicate) or annotate the result with the receiver's own element type — and narrow the elements after) is not supported yet

   8 | // Multi-arm narrowing: string | number is not a single arm.
   9 | const defined = xs.filter((x) => x !== undefined);
     |                 ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  10 | console.log(defined.length);

filter-narrow.ts:21:28 - error SC1090: narrowing '.filter' through a callback VALUE (only an inline callback whose predicate the checker inferred can re-tag — keep the receiver's element type instead — annotate the callback's return ': boolean' (the checker then skips the predicate) or annotate the result with the receiver's own element type — and narrow the elements after) is not supported yet

  20 | }
  21 | const viaValue = ys.filter(isString);
     |                            ^~~~~~~~
  22 | console.log(viaValue.length);