component-fences.ts:5:7 - error SC2009: values of type 'Map<boolean, string>' cannot be compiled: the Map shape is supported, but keys are limited to numbers and strings — 'boolean' is outside that domain

  4 | // Map keys are limited to numbers and strings.
  5 | const byFlag = new Map<boolean, string>();
    |       ^~~~~~
  6 | console.log(byFlag);

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

component-fences.ts:5:16 - error SC1090: Map keys of type 'boolean' (Map keys must be a string, a number, or a CLASS INSTANCE - those key by reference identity. A record/interface type cannot: a width coercion copies it, and a copy would miss its own entry) is not supported yet

  4 | // Map keys are limited to numbers and strings.
  5 | const byFlag = new Map<boolean, string>();
    |                ^~~~~~~~~~~~~~~~~~~~~~~~~~
  6 | console.log(byFlag);

component-fences.ts:6:13 - error SC2004: uses of 'byFlag' inherit the blocker on its declaration

  5 | const byFlag = new Map<boolean, string>();
  6 | console.log(byFlag);
    |             ^~~~~~
  7 | 

  hint: the declaration of 'byFlag' did not compile — fix the diagnostic reported there and these sites clear with it

component-fences.ts:10:7 - error SC2009: values of type 'Map<string, symbol>' cannot be compiled: the Map shape is supported, but 'symbol' values have no Map slot yet (symbols, bigints, generators and the runtime handle types stay out)

   9 | // index-signature overflow store's func row, now the user Map's too).
  10 | const listeners = new Map<string, symbol>();
     |       ^~~~~~~~~
  11 | console.log(listeners);

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

component-fences.ts:10:19 - error SC1090: Map values of type 'symbol' (Map values must be number, string, boolean, records, class instances, arrays, functions, promises, nested Maps/Sets, 'unknown', or unions of those — symbols, bigints, generators and the runtime handle types stay out) is not supported yet

   9 | // index-signature overflow store's func row, now the user Map's too).
  10 | const listeners = new Map<string, symbol>();
     |                   ^~~~~~~~~~~~~~~~~~~~~~~~~
  11 | console.log(listeners);

component-fences.ts:11:13 - error SC2004: uses of 'listeners' inherit the blocker on its declaration

  10 | const listeners = new Map<string, symbol>();
  11 | console.log(listeners);
     |             ^~~~~~~~~
  12 | 

  hint: the declaration of 'listeners' did not compile — fix the diagnostic reported there and these sites clear with it

component-fences.ts:14:7 - error SC2009: values of type 'Set<boolean>' cannot be compiled: the Set shape is supported, but elements are limited to numbers and strings — 'boolean' is outside that domain

  13 | // Set elements are limited to numbers and strings.
  14 | const flags = new Set<boolean>();
     |       ^~~~~
  15 | console.log(flags);

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

component-fences.ts:14:15 - error SC1090: Set elements of type 'boolean' (Set elements must be string or number — Map's key kinds — or a server handle, which stores under reference identity) is not supported yet

  13 | // Set elements are limited to numbers and strings.
  14 | const flags = new Set<boolean>();
     |               ^~~~~~~~~~~~~~~~~~
  15 | console.log(flags);

component-fences.ts:15:13 - error SC2004: uses of 'flags' inherit the blocker on its declaration

  14 | const flags = new Set<boolean>();
  15 | console.log(flags);
     |             ^~~~~
  16 | 

  hint: the declaration of 'flags' did not compile — fix the diagnostic reported there and these sites clear with it

component-fences.ts:35:7 - error SC2009: values of type '() => WeakMap<object, number>' cannot be compiled: the function shape is supported, but its return type 'WeakMap<object, number>' does not compile

  34 | // Node's ReferenceError at the root instead).
  35 | const makeWeak = (0 as unknown) as () => WeakMap<object, number>;
     |       ^~~~~~~~
  36 | const storedMake = makeWeak;

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

component-fences.ts:36:7 - error SC2009: values of type '() => WeakMap<object, number>' cannot be compiled: the function shape is supported, but its return type 'WeakMap<object, number>' does not compile

  35 | const makeWeak = (0 as unknown) as () => WeakMap<object, number>;
  36 | const storedMake = makeWeak;
     |       ^~~~~~~~~~
  37 | console.log(storedMake());

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

component-fences.ts:36:20 - error SC2004: uses of 'makeWeak' inherit the blocker on its declaration

  35 | const makeWeak = (0 as unknown) as () => WeakMap<object, number>;
  36 | const storedMake = makeWeak;
     |                    ^~~~~~~~
  37 | console.log(storedMake());

  hint: the declaration of 'makeWeak' did not compile — fix the diagnostic reported there and these sites clear with it

component-fences.ts:37:13 - error SC2004: uses of 'storedMake' inherit the blocker on its declaration

  36 | const storedMake = makeWeak;
  37 | console.log(storedMake());
     |             ^~~~~~~~~~
  38 | 

  hint: the declaration of 'storedMake' did not compile — fix the diagnostic reported there and these sites clear with it

component-fences.ts:44:7 - error SC2009: values of type 'Holder' cannot be compiled: the record shape is supported, but its member 'cache' has type 'WeakMap<object, number>', which does not compile

  43 | }
  44 | const held = (0 as unknown) as Holder;
     |       ^~~~
  45 | const kept = held;

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

component-fences.ts:45:7 - error SC2009: values of type 'Holder' cannot be compiled: the record shape is supported, but its member 'cache' has type 'WeakMap<object, number>', which does not compile

  44 | const held = (0 as unknown) as Holder;
  45 | const kept = held;
     |       ^~~~
  46 | console.log(kept.label);

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

component-fences.ts:45:14 - error SC2004: uses of 'held' inherit the blocker on its declaration

  44 | const held = (0 as unknown) as Holder;
  45 | const kept = held;
     |              ^~~~
  46 | console.log(kept.label);

  hint: the declaration of 'held' did not compile — fix the diagnostic reported there and these sites clear with it

component-fences.ts:46:13 - error SC2004: uses of 'kept' inherit the blocker on its declaration

  45 | const kept = held;
  46 | console.log(kept.label);
     |             ^~~~~~~~~~
  47 | 

  hint: the declaration of 'kept' did not compile — fix the diagnostic reported there and these sites clear with it