stdlib.ts:14:7 - error SC2005: values of type '{ <T extends string>(path: string, encoding: T): T extends "utf8" ? string : T extends "utf-8" ? string : any; (path: string): Buffer<ArrayBufferLike>; (fd: number, encoding: "utf-8" | "utf8"): string; (fd: number): Buffer<...>; (path: string, options: { ...; }): string; }' cannot be compiled: the signature keeps its type parameters, and a compiled function is always one concrete signature (generic functions monomorphize per pinned signature — declarations, methods, and never-reassigned bindings holding a generic arrow/function expression, an alias of a registered generic function, or a function initializer under a generic-signature annotation)

  13 | 
  14 | const read = readFileSync;
     |       ^~~~
  15 | const cwd = process.cwd;

  hint: annotate the destination with a concrete signature (e.g. '(x: number) => number'), instantiate explicitly ('f<number>'), or call the generic function directly

stdlib.ts:14:14 - error SC1090: library functions as values (call 'readFileSync' directly) are not supported yet

  13 | 
  14 | const read = readFileSync;
     |              ^~~~~~~~~~~~
  15 | const cwd = process.cwd;

stdlib.ts:15:13 - error SC1090: process methods as values (call 'cwd' directly) are not supported yet

  14 | const read = readFileSync;
  15 | const cwd = process.cwd;
     |             ^~~~~~~~~~~
  16 | const p = process;

stdlib.ts:18:12 - error SC1090: Math methods as values (call 'max' directly) are not supported yet

  17 | const env = process.env;
  18 | const mx = Math.max;
     |            ^~~~~~~~
  19 | const upper = "abc".toUpperCase;

stdlib.ts:19:15 - error SC1090: string methods as values (call 'toUpperCase' directly) are not supported yet

  18 | const mx = Math.max;
  19 | const upper = "abc".toUpperCase;
     |               ^~~~~~~~~~~~~~~~~
  20 | const fix = (1.5).toFixed;

stdlib.ts:20:13 - error SC1090: number methods as values (call 'toFixed' directly) are not supported yet

  19 | const upper = "abc".toUpperCase;
  20 | const fix = (1.5).toFixed;
     |             ^~~~~~~~~~~~~
  21 | const pf = parseFloat;