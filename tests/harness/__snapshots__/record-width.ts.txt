record-width.ts:26:20 - error SC2002: record shapes must match exactly or width-coerce: expected '{ m: ({ a: number; b: number }) => void }', got '{ extra: number; m: ({ a: number }) => void }' — field 'm': '({ a: number }) => void' does not lift into '({ a: number; b: number }) => void'

  25 | };
  26 | const dstM: DstM = srcM;
     |                    ^~~~
  27 | console.log(dstM);

  hint: width subtyping compiles as a copy (each expected field copied off the source; extra fields drop) — this pair is outside the copy's rules; build a literal with exactly the expected fields instead

record-width.ts:27:13 - error SC1090: console.log of '{ m: ({ a: number; b: number }) => void }' values (function values have no inspect lowering (no runtime name exists — Node prints '[Function: name]'; pass the function's declared identifier directly to util.inspect for the baked form)) is not supported yet

  26 | const dstM: DstM = srcM;
  27 | console.log(dstM);
     |             ^~~~
  28 | 

record-width.ts:40:18 - error SC2002: record shapes must match exactly or width-coerce: expected '{ a: boolean | undefined; [key: string]: boolean | string | undefined }', got '{ b: string }' — the expected field 'a' ('boolean | undefined') cannot take a runtime key collision from the 'boolean | string | undefined' signature slot

  39 | const plain = { b: "hi" };
  40 | const odd: Odd = plain;
     |                  ^~~~~
  41 | console.log(odd);

  hint: width subtyping compiles as a copy (each expected field copied off the source; extra fields drop) — this pair is outside the copy's rules; build a literal with exactly the expected fields instead

record-width.ts:41:13 - error SC1090: console.log of '{ a: boolean | undefined; [key: string]: boolean | string | undefined }' values (index-signature records with declared fields have no inspect lowering yet (JS interleaves integer keys across the declared and dynamic key sets)) are not supported yet

  40 | const odd: Odd = plain;
  41 | console.log(odd);
     |             ^~~
  42 | 