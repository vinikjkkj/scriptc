index-signature-values.ts:8:7 - error SC2006: values of type '{ [s: symbol]: number; }' cannot be compiled: this index signature is outside the supported shape (string or number keys; values limited to numbers, strings, booleans, records, classes, arrays, unions, functions, Maps, Sets, RegExps, Promises, 'unknown', or 'any')

  7 | // A symbol-keyed signature has no lowering.
  8 | const bySymbol: { [s: symbol]: number } = {};
    |       ^~~~~~~~
  9 | console.log(bySymbol);

  hint: string- and number-keyed index signatures over the supported value types compile directly (an 'any'- or 'unknown'-valued one rides the checked-dynamic store); symbol keys and dual signatures with unequal value types have no lowering

index-signature-values.ts:9:13 - error SC2004: uses of 'bySymbol' inherit the blocker on its declaration

   8 | const bySymbol: { [s: symbol]: number } = {};
   9 | console.log(bySymbol);
     |             ^~~~~~~~
  10 | 

  hint: the declaration of 'bySymbol' did not compile — fix the diagnostic reported there and these sites clear with it

index-signature-values.ts:13:13 - error SC1090: console.log of '{ [key: string]: Uint8Array }' values (typed arrays inside composites have no inspect lowering yet (Buffer's <Buffer ..> form vs Uint8Array's is not recorded in the static type here)) are not supported yet

  12 | const buffers: Record<string, Uint8Array> = {};
  13 | console.log(buffers);
     |             ^~~~~~~
  14 | 

index-signature-values.ts:18:7 - error SC2006: values of type '{ [k: string]: string | number; [n: number]: number; }' cannot be compiled: this index signature is outside the supported shape (string or number keys; values limited to numbers, strings, booleans, records, classes, arrays, unions, functions, Maps, Sets, RegExps, Promises, 'unknown', or 'any')

  17 | // answer per key).
  18 | const dual: { [k: string]: string | number; [n: number]: number } = {};
     |       ^~~~
  19 | console.log(dual);

  hint: string- and number-keyed index signatures over the supported value types compile directly (an 'any'- or 'unknown'-valued one rides the checked-dynamic store); symbol keys and dual signatures with unequal value types have no lowering

index-signature-values.ts:19:13 - error SC2004: uses of 'dual' inherit the blocker on its declaration

  18 | const dual: { [k: string]: string | number; [n: number]: number } = {};
  19 | console.log(dual);
     |             ^~~~
  20 | 

  hint: the declaration of 'dual' did not compile — fix the diagnostic reported there and these sites clear with it