blocked-binding-cascade.ts:9:5 - error SC2020: 'WeakMap<object, number>' is part of the standard library types but has no scriptc lowering yet

   8 | // battery used Symbol before symbol values grew a lowering.)
   9 | let w = new WeakMap<object, number>();
     |     ^
  10 | console.log(typeof w);

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

blocked-binding-cascade.ts:9:9 - error SC2020: 'new WeakMap' is part of the standard library types but has no scriptc lowering yet

   8 | // battery used Symbol before symbol values grew a lowering.)
   9 | let w = new WeakMap<object, number>();
     |         ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  10 | console.log(typeof w);

  hint: WeakMap is supported, but only for keys whose death this runtime observes at a single refcount chokepoint it owns — Uint8Array, and arrays of strings/numbers/booleans/byte-arrays. A record, a class instance, a bare 'object' or an array of those is a cycle-collected value that can be reclaimed without passing through a release, so its entry could outlive it; because keys are matched by ADDRESS and the allocator reuses addresses, that would return a dead key's value rather than merely leak, and it is refused instead. Values must be a reference type (a scalar has no pointer for the table to hold). A strong Map accepts those keys today, but it RETAINS them — for a cache keyed on a long-lived object that is unbounded growth

blocked-binding-cascade.ts:10:20 - error SC2004: uses of 'w' inherit the blocker on its declaration

   9 | let w = new WeakMap<object, number>();
  10 | console.log(typeof w);
     |                    ^
  11 | w = new WeakMap<object, number>();

  hint: the declaration of 'w' did not compile — fix the diagnostic reported there and these sites clear with it

blocked-binding-cascade.ts:11:1 - error SC2004: uses of 'w' inherit the blocker on its declaration

  10 | console.log(typeof w);
  11 | w = new WeakMap<object, number>();
     | ^
  12 | 

  hint: the declaration of 'w' did not compile — fix the diagnostic reported there and these sites clear with it

blocked-binding-cascade.ts:16:10 - error SC2020: 'WeakMap<object, number>' is part of the standard library types but has no scriptc lowering yet

  15 | // use itself cascades.
  16 | function makeWm(): WeakMap<object, number> {
     |          ^~~~~~
  17 |   return new WeakMap<object, number>();

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

blocked-binding-cascade.ts:19:7 - error SC2009: values of type '() => WeakMap<object, number>' cannot be compiled: the function shape is supported, but its return type 'WeakMap<object, number>' does not compile

  18 | }
  19 | const f = makeWm;
     |       ^
  20 | 

  hint: the outer shape is supported — the named component is the blocker; restate it inside the compilable set (number, string, boolean, arrays, Maps, Sets, RegExp, functions, classes, records, unions of those, and 'unknown')

blocked-binding-cascade.ts:19:11 - error SC2004: uses of 'makeWm' inherit the blocker on its declaration

  18 | }
  19 | const f = makeWm;
     |           ^~~~~~
  20 | 

  hint: the declaration of 'makeWm' did not compile — fix the diagnostic reported there and these sites clear with it