generics.ts:23:3 - error SC1090: generic nested functions (only top-level generic function declarations are supported) are not supported yet

  22 | function outer(): void {
  23 |   function nested<T>(x: T): T {
     |   ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  24 |     return x;

generics.ts:30:10 - error SC1090: '.join()' on arrays of this element type (number, string, and boolean arrays join — unions of those with undefined/null arms too, the units printing empty like JS) is not supported yet (instantiating 'joinAll' with <number[]>)

  29 | function joinAll<T>(a: T[]): string {
  30 |   return a.join(",");
     |          ^~~~~~~~~~~
  31 | }

generics.ts:44:18 - error SC1090: generic classes as values ('typeof Box' keeps the type parameter — instantiation expressions ('Box<number>') and concrete constructor-typed slots compile) are not supported yet

  43 | // The uninstantiated family as a value: no thunk, no single ctor ABI.
  44 | const BoxAlias = Box;
     |                  ^~~
  45 | // A base that mentions the class's own type parameters differs per

generics.ts:56:10 - error SC1090: 'instanceof Chained' where the class's 'extends' clause mentions its own type parameters (each instantiation extends a different base, so no one interval covers the family the way JS's single runtime class does) are not supported yet

  55 | function chainedInstanceOf(v: Chained<number>): boolean {
  56 |   return v instanceof Chained;
     |          ^~~~~~~~~~~~~~~~~~~~
  57 | }

generics.ts:60:20 - error SC1090: generic class expressions are not supported yet

  59 | // Generic class expressions: each evaluation would mint a distinct FAMILY.
  60 | const ExprFamily = class <T> {
     |                    ^~~~~~~~~~~
  61 |   x: T | undefined;

generics.ts:74:1 - error SC1090: constructing through a class value whose class has no lowering (the class declaration itself was rejected — see its own diagnostic) is not supported yet

  73 | new Chained(2);
  74 | new ExprFamily<number>();
     | ^~~~~~~~~~~~~~~~~~~~~~~~
  75 | 