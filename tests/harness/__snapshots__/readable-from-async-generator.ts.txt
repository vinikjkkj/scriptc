readable-from-async-generator.ts:39:24 - error SC2020: 'Readable.from over a 'AsyncGenerator<Uint8Array, void, unknown>' source' is part of the standard library types but has no scriptc lowering yet

  38 | export function make(): Readable {
  39 |   return Readable.from(chunks());
     |                        ^~~~~~~~
  40 | }

  hint: string[] / Buffer[] arrays (and a single string or Buffer) are the supported sources — generators and async iterables have no lowering yet

readable-from-async-generator.ts:56:24 - error SC2020: 'Readable.from over a 'Generator<Uint8Array, void, unknown>' source' is part of the standard library types but has no scriptc lowering yet

  55 | export function makeSync(): Readable {
  56 |   return Readable.from(syncChunks());
     |                        ^~~~~~~~~~~~
  57 | }

  hint: string[] / Buffer[] arrays (and a single string or Buffer) are the supported sources — generators and async iterables have no lowering yet