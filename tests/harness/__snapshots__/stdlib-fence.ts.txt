stdlib-fence.ts:10:14 - error SC2020: 'Symbol.iterator' is part of the standard library types but has no scriptc lowering yet

   9 | // surface below.)
  10 | const iter = Symbol.iterator;
     |              ^~~~~~~~~~~~~~~
  11 | const g = globalThis;

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

stdlib-fence.ts:15:15 - error SC1090: statically-decided Array.isArray on computed arguments (bind the value to a variable first) are not supported yet

  14 | const n5 = Number.NaN; // lowers now, with the other Number constants
  15 | const isArr = Array.isArray([1]);
     |               ^~~~~~~~~~~~~~~~~~
  16 | const merged = Object.assign({}, { a: 1 }); // the empty-target literal-source shape lowers now (it IS the source literal)

stdlib-fence.ts:17:23 - error SC2020: 'Object.assign' is part of the standard library types but has no scriptc lowering yet

  16 | const merged = Object.assign({}, { a: 1 }); // the empty-target literal-source shape lowers now (it IS the source literal)
  17 | const aliasedAssign = Object.assign(merged, { b: 2 }); // aliased targets stay fenced (real mutation of a live object)
     |                       ^~~~~~~~~~~~~
  18 | const refl = Reflect.has({ a: 1 }, "a");

  hint: spread instead: { ...a, ...b } builds the merged record; what lowers: the empty-target literal-source shape (Object.assign({}, { ... }) IS the source literal) and merges INTO an index-signature record whose value slot every source value enters — other aliased targets are real mutation, and a function target (Object.assign(fn, { prop })) is a function-with-properties value the model cannot represent: bind the property separately

stdlib-fence.ts:18:14 - error SC2020: 'Reflect.has' is part of the standard library types but has no scriptc lowering yet

  17 | const aliasedAssign = Object.assign(merged, { b: 2 }); // aliased targets stay fenced (real mutation of a live object)
  18 | const refl = Reflect.has({ a: 1 }, "a");
     |              ^~~~~~~~~~~
  19 | const parsedMs = Date.parse("2024-01-01");

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

stdlib-fence.ts:19:18 - error SC2020: 'Date.parse' is part of the standard library types but has no scriptc lowering yet

  18 | const refl = Reflect.has({ a: 1 }, "a");
  19 | const parsedMs = Date.parse("2024-01-01");
     |                  ^~~~~~~~~~
  20 | const fmt = new Intl.NumberFormat();

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

stdlib-fence.ts:20:7 - error SC2020: 'NumberFormat' is part of the standard library types but has no scriptc lowering yet

  19 | const parsedMs = Date.parse("2024-01-01");
  20 | const fmt = new Intl.NumberFormat();
     |       ^~~
  21 | 

  hint: Intl formatter values have no representation — the COMPOSED en-US forms lower: new Intl.NumberFormat("en-US").format(x) and x.toLocaleString("en-US") with default options; the rest is ICU locale data the binary does not carry

stdlib-fence.ts:20:13 - error SC1090: constructing values other than classes declared in the program is not supported yet

  19 | const parsedMs = Date.parse("2024-01-01");
  20 | const fmt = new Intl.NumberFormat();
     |             ^~~~~~~~~~~~~~~~~~~~~~~
  21 | 

stdlib-fence.ts:23:11 - error SC2020: 'new Date' is part of the standard library types but has no scriptc lowering yet

  22 | // Constructors.
  23 | const d = new Date();
     |           ^~~~~~~~~~
  24 | const wm = new WeakMap();

  hint: Date values have no representation — Date.now() and the composed new Date(ms?).toISOString() form compile

stdlib-fence.ts:24:7 - error SC2020: 'WeakMap<WeakKey, any>' is part of the standard library types but has no scriptc lowering yet

  23 | const d = new Date();
  24 | const wm = new WeakMap();
     |       ^~
  25 | const px = new Proxy({ a: 1 }, {});

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

stdlib-fence.ts:24:12 - error SC2020: 'new WeakMap' is part of the standard library types but has no scriptc lowering yet

  23 | const d = new Date();
  24 | const wm = new WeakMap();
     |            ^~~~~~~~~~~~~
  25 | const px = new Proxy({ a: 1 }, {});

  hint: weak collections observe garbage collection, which reference counting never exposes — a strong Map behaves identically in-language: use Map

stdlib-fence.ts:25:12 - error SC2020: 'new Proxy' is part of the standard library types but has no scriptc lowering yet

  24 | const wm = new WeakMap();
  25 | const px = new Proxy({ a: 1 }, {});
     |            ^~~~~~~~~~~~~~~~~~~~~~~
  26 | const buf = new ArrayBuffer(8);

  hint: property-access metaprogramming has no static lowering (every property read must resolve at compile time)

stdlib-fence.ts:26:13 - error SC2020: 'new ArrayBuffer' is part of the standard library types but has no scriptc lowering yet

  25 | const px = new Proxy({ a: 1 }, {});
  26 | const buf = new ArrayBuffer(8);
     |             ^~~~~~~~~~~~~~~~~~
  27 | const st = new Set(new Set([1, 2])); // array seeds lower; Set/iterable seeds stay fenced

  hint: no free-standing ArrayBuffer value exists — typed arrays own their storage: allocate the view directly (new Uint8Array(n)), or erase a fresh buffer into one (new Uint8Array(new ArrayBuffer(n)), new DataView(new ArrayBuffer(n), ...))

stdlib-fence.ts:31:7 - error SC2020: 'AggregateError' is part of the standard library types but has no scriptc lowering yet

  30 | // and the unlowered members of lowered error objects.)
  31 | const agg = new AggregateError([]);
     |       ^~~
  32 | // (`.stack` READS now lower — the header line a zero-frame capture

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

stdlib-fence.ts:31:13 - error SC2020: 'new AggregateError' is part of the standard library types but has no scriptc lowering yet

  30 | // and the unlowered members of lowered error objects.)
  31 | const agg = new AggregateError([]);
     |             ^~~~~~~~~~~~~~~~~~~~~~
  32 | // (`.stack` READS now lower — the header line a zero-frame capture

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

stdlib-fence.ts:35:15 - error SC2020: 'Error.cause' is part of the standard library types but has no scriptc lowering yet

  34 | // error object is `.cause`, whose slot the ScrError layout does not carry.)
  35 | const cause = new Error("boom").cause;
     |               ^~~~~~~~~~~~~~~~~~~~~~~
  36 | 

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

stdlib-fence.ts:42:7 - error SC2020: 'ArrayIterator<[number, number]>' is part of the standard library types but has no scriptc lowering yet

  41 | // fences any more; `Math.SQRT2` above is the Math member that still does.)
  42 | const entries = [1, 2].entries();
     |       ^~~~~~~
  43 | const arrAt = [1, 2].at(0);

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

stdlib-fence.ts:42:17 - error SC2020: 'number[].entries' is part of the standard library types but has no scriptc lowering yet

  41 | // fences any more; `Math.SQRT2` above is the Math member that still does.)
  42 | const entries = [1, 2].entries();
     |                 ^~~~~~~~~~~~~~
  43 | const arrAt = [1, 2].at(0);

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

stdlib-fence.ts:44:14 - error SC2020: 'string.normalize' is part of the standard library types but has no scriptc lowering yet

  43 | const arrAt = [1, 2].at(0);
  44 | const norm = "abc".normalize();
     |              ^~~~~~~~~~~~~~~
  45 | const limited = "a,b,c".split(",", 2); // the string-separator split lowers; the limit form stays fenced

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

stdlib-fence.ts:47:19 - error SC2020: 'Number.prototype.toLocaleString without a locale' is part of the standard library types but has no scriptc lowering yet

  46 | const fixed = (1.5).toFixed(); // digit-free toFixed lowers now (the static ties-up integer)
  47 | const localized = (1234.5).toLocaleString();
     |                   ^~~~~~~~~~~~~~~~~~~~~~~~~
  48 | 

  hint: the default locale is the host environment's, which a compiled binary cannot carry — pass it explicitly: x.toLocaleString("en-US")

stdlib-fence.ts:52:15 - error SC2020: '.indexOf with 2 arguments' is part of the standard library types but has no scriptc lowering yet

  51 | const nums = [1, 2, 3];
  52 | const found = nums.indexOf(2, 1);
     |               ^~~~~~~~~~~~~~~~~~
  53 | const joined = nums.join();

  hint: the fromIndex parameter has no lowering — slice first, or loop

stdlib-fence.ts:53:16 - error SC2020: '.join with 0 arguments' is part of the standard library types but has no scriptc lowering yet

  52 | const found = nums.indexOf(2, 1);
  53 | const joined = nums.join();
     |                ^~~~~~~~~~~
  54 | nums.forEach((x) => console.log(x), { unused: true }); // thisArg stays fenced

  hint: pass the separator explicitly: join(",")

stdlib-fence.ts:54:1 - error SC2020: '.forEach with 2 arguments' is part of the standard library types but has no scriptc lowering yet

  53 | const joined = nums.join();
  54 | nums.forEach((x) => console.log(x), { unused: true }); // thisArg stays fenced
     | ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  55 | const pos = "abc".includes("b", 1); // the position form lowers now (indexOf's clamp)

  hint: the thisArg parameter has no lowering — use an arrow function

stdlib-fence.ts:56:13 - error SC2020: 'JSON.parse with a reviver' is part of the standard library types but has no scriptc lowering yet

  55 | const pos = "abc".includes("b", 1); // the position form lowers now (indexOf's clamp)
  56 | const rev = JSON.parse("1", (_k, v) => v);
     |             ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  57 | // (stringify with a null replacer + LITERAL space now lowers — the pretty

  hint: parse to `unknown` and validate with a checked cast ('as T') instead

stdlib-fence.ts:61:18 - error SC2020: 'JSON.stringify with replacer/space parameters' is part of the standard library types but has no scriptc lowering yet

  60 | const toStringify = { a: 1 };
  61 | const replaced = JSON.stringify(toStringify, (_k, v) => v);
     |                  ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  62 | const width = 2;

  hint: the serializer is type-directed — shape the value before stringifying

stdlib-fence.ts:63:16 - error SC2020: 'JSON.stringify with replacer/space parameters' is part of the standard library types but has no scriptc lowering yet

  62 | const width = 2;
  63 | const spaced = JSON.stringify(toStringify, null, width);
     |                ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  64 | 

  hint: the serializer is type-directed — shape the value before stringifying

stdlib-fence.ts:76:15 - error SC2020: 'Object.is over a dynamic operand' is part of the standard library types but has no scriptc lowering yet

  75 | // unions needing narrowing.
  76 | const dynIs = Object.is(JSON.parse("1") as unknown, 1);
     |               ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  77 | function pick(a: string | number, b: number | boolean): boolean {

  hint: validate/narrow the value first (strict equality's rule) — SameValue only differs from === on numbers (NaN, ±0)

stdlib-fence.ts:78:10 - error SC2020: 'Object.is over these union operands' is part of the standard library types but has no scriptc lowering yet

  77 | function pick(a: string | number, b: number | boolean): boolean {
  78 |   return Object.is(a, b); // partially-overlapping unions: narrow first
     |          ^~~~~~~~~~~~~~~
  79 | }

  hint: union-typed comparisons need one comparable shape (narrow first: check a discriminant field, or compare with '!== undefined'/'!== null' for unit arms)