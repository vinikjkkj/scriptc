fenced.ts:5:13 - error SC2020: 'process.memoryUsage' is typed by @types/node but has no scriptc lowering yet

  4 |  * never a silently broken binary. */
  5 | console.log(process.memoryUsage()); // uptime/cpuUsage/resourceUsage lower now; the V8-heap report does not
    |             ^~~~~~~~~~~~~~~~~~~
  6 | const buf = Buffer.from("bytes").readBigUInt64BE(0); // from, fill, and the numeric families lower; the bigint reads fence

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:6:13 - error SC2020: 'Buffer<ArrayBuffer>.readBigUInt64BE' is typed by @types/node but has no scriptc lowering yet

  5 | console.log(process.memoryUsage()); // uptime/cpuUsage/resourceUsage lower now; the V8-heap report does not
  6 | const buf = Buffer.from("bytes").readBigUInt64BE(0); // from, fill, and the numeric families lower; the bigint reads fence
    |             ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  7 | console.log(Buffer.poolSize);

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:7:13 - error SC2020: 'Buffer.poolSize' is typed by @types/node but has no scriptc lowering yet

  6 | const buf = Buffer.from("bytes").readBigUInt64BE(0); // from, fill, and the numeric families lower; the bigint reads fence
  7 | console.log(Buffer.poolSize);
    |             ^~~~~~~~~~~~~~~
  8 | /* setInterval lowers, and under @types/node its Timeout RETURN now maps

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:17:1 - error SC2020: 'Timeout.close' is typed by @types/node but has no scriptc lowering yet

  16 | timer.refresh();
  17 | timer.close();
     | ^~~~~~~~~~~~~
  18 | /* The web-platform globals ride in with @types/node (undici): they RESOLVE

  hint: unref(), ref(), hasRef(), and refresh() are the supported Timeout methods

fenced.ts:26:42 - error SC2020: 'fetch init option 'redirect'' is part of the standard library types but has no scriptc lowering yet

  25 | controller.abort();
  26 | void fetch("https://example.invalid/", { redirect: "manual" });
     |                                          ^~~~~~~~~~~~~~~~~~
  27 | /* Members of SUPPORTED builtin modules beyond the lowered tables: they

  hint: this fetch always follows redirects (fetch's 20-hop default); 'manual' and 'error' would need the hop to stop mid-flight

fenced.ts:33:1 - error SC2020: 'fs.watchFile' is typed by @types/node but has no scriptc lowering yet

  32 | import { win32 } from "path";
  33 | watchFile("x", () => {});
     | ^~~~~~~~~~~~~~~~~~~~~~~~
  34 | console.log(cpus().length);

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:34:13 - error SC2020: 'os.cpus' is typed by @types/node but has no scriptc lowering yet

  33 | watchFile("x", () => {});
  34 | console.log(cpus().length);
     |             ^~~~~~
  35 | console.log(win32.sep);

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:35:13 - error SC2020: 'PlatformPath.sep' is typed by @types/node but has no scriptc lowering yet

  34 | console.log(cpus().length);
  35 | console.log(win32.sep);
     |             ^~~~~~~~~
  36 | /* URL members beyond the supported getters (protocol/pathname/href/

  hint: the type checker sees everything @types/node declares, but only the supported surface compiles (https://scriptc.dev/limitations)

fenced.ts:41:13 - error SC2020: 'URL.hash' is typed by @types/node but has no scriptc lowering yet

  40 | const u = new URL("https://example.com/x?a=1");
  41 | console.log(u.hash);
     |             ^~~~~~
  42 | u.searchParams.get("a");

  hint: protocol, pathname, href, host, hostname, search, searchParams, and toString() are the supported URL members

fenced.ts:48:13 - error SC2020: 'deflateSync of 'string' data' is part of the standard library types but has no scriptc lowering yet

  47 | import { deflateSync, gzipSync } from "zlib";
  48 | deflateSync("data");
     |             ^~~~~~
  49 | gzipSync("data");

  hint: zlib works on Buffers: deflateSync(Buffer.from(s, "utf8"))

fenced.ts:49:10 - error SC2020: 'gzipSync of 'string' data' is part of the standard library types but has no scriptc lowering yet

  48 | deflateSync("data");
  49 | gzipSync("data");
     |          ^~~~~~
  50 | /* The http2 compatibility slice's @types/node-world fences (divergence

  hint: zlib works on Buffers: gzipSync(Buffer.from(s, "utf8"))

fenced.ts:58:71 - error SC2020: 'a createSecureServer SNICallback of 'undefined' values' is part of the standard library types but has no scriptc lowering yet

  57 | import * as http2 from "node:http2";
  58 | http2.createSecureServer({ allowHTTP1: true, cert: "pem", key: "pem", SNICallback: undefined });
     |                                                                       ^~~~~~~~~~~~~~~~~~~~~~
  59 | http2.createSecureServer({

  hint: SNICallback lowers as a direct option or the conditional spread ...(x ? { SNICallback: x } : {}) — the callback is (servername, cb) => void with cb: (err: Error | null, ctx?: tls.SecureContext) => void

fenced.ts:63:3 - error SC2020: 'createSecureServer options with a conditional spread' is part of the standard library types but has no scriptc lowering yet

  62 |   key: "pem",
  63 |   ...(1 ? { SNICallback: undefined } : {}),
     |   ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  64 | });

  hint: the lowered conditional spread is exactly ...(x ? { SNICallback: x } : {}) — the included value must BE the condition

fenced.ts:65:71 - error SC2020: 'createSecureServer option 'streamResetBurst' with a non-literal value' is part of the standard library types but has no scriptc lowering yet

  64 | });
  65 | http2.createSecureServer({ allowHTTP1: true, cert: "pem", key: "pem", streamResetBurst: 1 + 0 });
     |                                                                       ^~~~~~~~~~~~~~~~~~~~~~~
  66 | http2.connect("https://localhost");

  hint: h2 session-tuning options are accepted with literal values (settings: a literal of literals) and ignored — the allowHTTP1 lowering serves HTTP/1.1 only, which has no h2 sessions to tune

fenced.ts:72:1 - error SC2020: 'crypto.generateKeyPair' is typed by @types/node but has no scriptc lowering yet

  71 | import { createCipheriv, generateKeyPair, pbkdf2Sync, setFips } from "node:crypto";
  72 | generateKeyPair("rsa", { modulusLength: 2048 }, () => {});
     | ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  73 | createCipheriv("aes-128-cbc", Buffer.alloc(16), Buffer.alloc(16));

  hint: asymmetric-key operations need a public-key stack (bignum, RSA/EC/EdDSA math) and a KeyObject value model — neither exists in the static runtime, so no faithful lowering can be small; the lowered crypto surface is hashing, randomness, and the introspection statics

fenced.ts:73:1 - error SC2020: 'createCipheriv with this algorithm' is part of the standard library types but has no scriptc lowering yet

  72 | generateKeyPair("rsa", { modulusLength: 2048 }, () => {});
  73 | createCipheriv("aes-128-cbc", Buffer.alloc(16), Buffer.alloc(16));
     | ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  74 | pbkdf2Sync("pw", "salt", 100000, 64, "sha512");

  hint: aes-256-gcm, aes-256-cbc and aes-256-ctr are the lowered ciphers (the options argument has no lowering either)

fenced.ts:74:38 - error SC2020: 'crypto.pbkdf2Sync with this digest' is part of the standard library types but has no scriptc lowering yet

  73 | createCipheriv("aes-128-cbc", Buffer.alloc(16), Buffer.alloc(16));
  74 | pbkdf2Sync("pw", "salt", 100000, 64, "sha512");
     |                                      ^~~~~~~~
  75 | setFips(false);

  hint: sha256 is the derived PRF — pass it as a literal (another digest would derive a different key)

fenced.ts:75:1 - error SC2020: 'crypto.setFips' is typed by @types/node but has no scriptc lowering yet

  74 | pbkdf2Sync("pw", "salt", 100000, 64, "sha512");
  75 | setFips(false);
     | ^~~~~~~~~~~~~~
  76 | 

  hint: a compiled binary has no FIPS provider to enable, and Node itself throws on setFips(true) in a non-FIPS build — getFips() answers 0 here