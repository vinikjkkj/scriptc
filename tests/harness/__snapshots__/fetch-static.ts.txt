fetch-static.ts:27:13 - error SC2012: 'AbortSignal.timeout' runs in the embedded dynamic engine, which this build does not include

  26 | }
  27 | const sig = AbortSignal.timeout(100);
     |             ^~~~~~~~~~~~~~~~~~~~~~~~
  28 | async function timed(url: string): Promise<string> {

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

fetch-static.ts:29:40 - error SC2012: 'AbortSignal.timeout' runs in the embedded dynamic engine, which this build does not include

  28 | async function timed(url: string): Promise<string> {
  29 |   const r = await fetch(url, { signal: AbortSignal.timeout(100) });
     |                                        ^~~~~~~~~~~~~~~~~~~~~~~~
  30 |   return r.text();

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

fetch-static.ts:34:10 - error SC2020: 'Response.body' is part of the standard library types but has no scriptc lowering yet

  33 |   const r = await fetch(url);
  34 |   return r.body !== null;
     |          ^~~~~~
  35 | }

  hint: this build has no ReadableStream body — read the body with text(), json(), arrayBuffer() or bytes()

fetch-static.ts:39:13 - error SC1090: console.log of '(Response) => boolean' values (function values have no inspect lowering (no runtime name exists — Node prints '[Function: name]'; pass the function's declared identifier directly to util.inspect for the baked form)) are not supported yet

  38 | streamed("http://localhost/c");
  39 | console.log(inspect, sig);
     |             ^~~~~~~
  40 | 