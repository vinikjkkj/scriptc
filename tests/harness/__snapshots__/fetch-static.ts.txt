fetch-static.ts:6:19 - error SC2012: 'fetch' runs in the embedded dynamic engine, which this build does not include

  5 | async function probe(url: string): Promise<number> {
  6 |   const r = await fetch(url);
    |                   ^~~~~~~~~~
  7 |   return r.status;

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

fetch-static.ts:7:10 - error SC2020: 'Response.status' is part of the standard library types but has no scriptc lowering yet

  6 |   const r = await fetch(url);
  7 |   return r.status;
    |          ^~~~~~~~
  8 | }

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)

fetch-static.ts:12:13 - error SC2012: 'AbortSignal.timeout' runs in the embedded dynamic engine, which this build does not include

  11 | }
  12 | const sig = AbortSignal.timeout(100);
     |             ^~~~~~~~~~~~~~~~~~~~~~~~
  13 | async function timed(url: string): Promise<string> {

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

fetch-static.ts:14:19 - error SC2012: 'fetch' runs in the embedded dynamic engine, which this build does not include

  13 | async function timed(url: string): Promise<string> {
  14 |   const r = await fetch(url, { signal: AbortSignal.timeout(100) });
     |                   ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  15 |   return r.text();

  hint: build with --dynamic to run this call in the embedded engine (adds ~620KB to the binary); static builds never include it

fetch-static.ts:15:10 - error SC2020: 'Response.text' is part of the standard library types but has no scriptc lowering yet

  14 |   const r = await fetch(url, { signal: AbortSignal.timeout(100) });
  15 |   return r.text();
     |          ^~~~~~
  16 | }

  hint: the type checker sees the full standard library, but only the supported surface compiles (https://scriptc.dev/limitations)