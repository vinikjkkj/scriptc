fetch-typed-option-member.ts:57:5 - error SC2020: 'RequestInit.dispatcher' is part of the standard library types but has no scriptc lowering yet

  56 |   if (dispatcher) {
  57 |     (init as { dispatcher?: unknown }).dispatcher = dispatcher;
     |     ^~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
  58 |   }

  hint: undici's dispatcher is an engine object driving a callback protocol (dispatch(opts, handler)), and Node really does call it — there is no static representation for one; this build's fetch dials the origin directly and has no proxy path, environment-configured or otherwise