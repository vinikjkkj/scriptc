// DefinitelyTyped-style declarations for the sibling runtime package
// `dtwin`. This package ships no code at all — it cannot run anywhere, in
// the embedded engine or otherwise — so no diagnostic may name it as the
// thing a value or an import needs.
export declare class Pool {
    constructor(size: number);
    readonly size: number;
    describe(): string;
}
