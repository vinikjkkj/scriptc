"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
// The PUBLISHED build: the alias is already resolved away, which is why
// Node runs this happily and only the source tree needs the table.
const CORE = "core-1.0.0";
function spin(s) {
  return "[" + CORE + ":" + s.toUpperCase() + "]";
}
exports.spin = spin;
