/* The attested SOURCE of hub's root entry — the only hub specifier the
 * driver imports directly, and therefore the only one the pipeline used
 * to know about when hub's entry table was built. */
export const HUB = "hub-3.0.0";
