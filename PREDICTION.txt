block/libsurface — PREDICTION, written BEFORE any measurement
base main d31c34b9   worktree G:\scriptc-ls   caches zig-ls-local / scriptc-ls / provenance-ls

Rows in my block: 15 (Readable.from over an async generator + SC1071)
                  19 (Request/Response as IrTypes)

P1  Row 15's recorded decliner is lower-stream.ts:1238 L.noLowering
    "Readable.from over a '?' source". I predict the stack dump CONFIRMS
    that site (it is the only noLowering with that wording).
P2  The '?' means mapTypeOf(typeOf(args[0])) returned NULL for
    AsyncGenerator<Uint8Array>, i.e. mapType has no async-generator case at
    all. Frame above = lowerBuiltinStreamStatic in lower-stream.ts.
P3  Row 15's capability is ABSENT, not unreachable: there is a
    lower-generators.ts for SYNC generators (fiber-based, genResume) and
    nothing for async ones. Closing it needs (a) an async-generator
    lowering and (b) a readable.fromAsyncIter libCall on both backends.
    PREDICTION: I will REFUSE row 15 and price it.
P4  Row 19's FIRST decliner is the ISLAND_AMBIENT_TYPES gate. The two
    sites are frontend/types.ts:2162 (mapType) and
    lowering/lowerer.ts:2958 (badType wording). Leaf = `Request` inside
    `typeof fetch` = (input: RequestInfo|URL, init?: RequestInit) =>
    Promise<Response>, RequestInfo = Request | string.
P5  Row 19's SECOND decliner, per the survey, is `options.fetch ?? fetch`
    where bare `fetch` as a VALUE misses lower-island.ts's lowerFetchCall
    (call-form only). I predict the stack dump names a DIFFERENT site than
    the survey's :248 — five of the last closures were found because the
    recorded frame was wrong or one frame off.
P6  Cheap-shape check: I predict row 19 is NOT cheap — `Request` really is
    absent from the four-name list and adding a name to a list is exactly
    the "widen a gate" move the brief forbids without a counterexample.
    But I predict the ROW-15 side has a cheap sub-shape I have not seen
    named: `Readable.from` over a SYNC generator (SC2020) may already be
    reachable, since lower-generators.ts exists and genResume works.
P7  Neither row is closed by me. I predict I move 0 of 2, and that both
    refusals stand with a counterexample. (Scored in the report.)
P8  Census after my change is IDENTICAL to base on every category:
    refusals 15, ABORT.real 23/94, ABORT.structural 3773, boilerplate 85,
    DYNCHECK 2254, parity 0, unknown 0.
P9  No accounting test or corpus program asserts either row (I have not
    checked yet; I predict tests/diagnostics has a fetch-typed-option-member
    test that DOES pin row 19 as correct — the survey says so — and that
    nothing pins row 15).
